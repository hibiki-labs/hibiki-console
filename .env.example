# Session Configuration
SESSION_COOKIE_NAME=hibiki_console
SESSION_COOKIE_PASSWORD=your-secure-password-at-least-32-characters-long
SESSION_MAX_AGE_SECONDS=999999999

# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=hibiki_console
DB_USER_NAME=hibiki_console
DB_USER_PASS=hibiki_console

# Authentication Configuration
# Set to 'true' to enable OTP (two-factor authentication) during login
# Set to 'false' to skip OTP step and login with credentials only
ENABLE_OTP=true
